--[[
  On Chain Dapps - REST API

   This section will provide necessary information about the `OnChain API` protocol.  <br/><br/> Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.                             

  The version of the OpenAPI document: v1
  Contact: support@coinapi.io
  Generated by: https://openapi-generator.tech
]]

--package openapiclient

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import
local openapiclient_pancakeswap_v3_ethereum_account_dto = require "openapiclient.model.pancakeswap_v3_ethereum_account_dto"
local openapiclient_pancakeswap_v3_ethereum_active_account_dto = require "openapiclient.model.pancakeswap_v3_ethereum_active_account_dto"
local openapiclient_pancakeswap_v3_ethereum_deposit_dto = require "openapiclient.model.pancakeswap_v3_ethereum_deposit_dto"
local openapiclient_pancakeswap_v3_ethereum_dex_amm_protocol_dto = require "openapiclient.model.pancakeswap_v3_ethereum_dex_amm_protocol_dto"
local openapiclient_pancakeswap_v3_ethereum_financials_daily_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_financials_daily_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_liquidity_pool_amount_dto = require "openapiclient.model.pancakeswap_v3_ethereum_liquidity_pool_amount_dto"
local openapiclient_pancakeswap_v3_ethereum_liquidity_pool_dto = require "openapiclient.model.pancakeswap_v3_ethereum_liquidity_pool_dto"
local openapiclient_pancakeswap_v3_ethereum_liquidity_pool_daily_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_liquidity_pool_daily_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_liquidity_pool_fee_dto = require "openapiclient.model.pancakeswap_v3_ethereum_liquidity_pool_fee_dto"
local openapiclient_pancakeswap_v3_ethereum_liquidity_pool_hourly_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_liquidity_pool_hourly_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_position_dto = require "openapiclient.model.pancakeswap_v3_ethereum_position_dto"
local openapiclient_pancakeswap_v3_ethereum_position_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_position_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_reward_token_dto = require "openapiclient.model.pancakeswap_v3_ethereum_reward_token_dto"
local openapiclient_pancakeswap_v3_ethereum_swap_dto = require "openapiclient.model.pancakeswap_v3_ethereum_swap_dto"
local openapiclient_pancakeswap_v3_ethereum_tick_dto = require "openapiclient.model.pancakeswap_v3_ethereum_tick_dto"
local openapiclient_pancakeswap_v3_ethereum_tick_daily_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_tick_daily_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_tick_hourly_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_tick_hourly_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_token_dto = require "openapiclient.model.pancakeswap_v3_ethereum_token_dto"
local openapiclient_pancakeswap_v3_ethereum_token_white_list_dto = require "openapiclient.model.pancakeswap_v3_ethereum_token_white_list_dto"
local openapiclient_pancakeswap_v3_ethereum_token_white_list_symbol_dto = require "openapiclient.model.pancakeswap_v3_ethereum_token_white_list_symbol_dto"
local openapiclient_pancakeswap_v3_ethereum_usage_metrics_daily_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_usage_metrics_daily_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_usage_metrics_hourly_snapshot_dto = require "openapiclient.model.pancakeswap_v3_ethereum_usage_metrics_hourly_snapshot_dto"
local openapiclient_pancakeswap_v3_ethereum_withdraw_dto = require "openapiclient.model.pancakeswap_v3_ethereum_withdraw_dto"

local pancakeswapv3_ethereum_api = {}
local pancakeswapv3_ethereum_api_mt = {
	__name = "pancakeswapv3_ethereum_api";
	__index = pancakeswapv3_ethereum_api;
}

local function new_pancakeswapv3_ethereum_api(authority, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	local host, port = http_util.split_authority(authority, default_scheme)
	return setmetatable({
		host = host;
		port = port;
		basePath = basePath or "https://onchain.coinapi.io";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, pancakeswapv3_ethereum_api_mt)
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_accounts__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/accounts/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_account_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_active_accounts__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/activeAccounts/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_active_account_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_deposits__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/deposits/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_deposit_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_dex_amm_protocols__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/dexAmmProtocols/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_dex_amm_protocol_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_financials_daily_snapshots__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/financialsDailySnapshots/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_financials_daily_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_liquidity_pool_amounts__current(id)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/liquidityPoolAmounts/current?id=%s",
			self.basePath, http_util.encodeURIComponent(id));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_liquidity_pool_amount_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_liquidity_pool_daily_snapshots__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/liquidityPoolDailySnapshots/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_liquidity_pool_daily_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_liquidity_pool_fees__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/liquidityPoolFees/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_liquidity_pool_fee_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_liquidity_pool_hourly_snapshots__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/liquidityPoolHourlySnapshots/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_liquidity_pool_hourly_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_liquidity_pools__current(id)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/liquidityPools/current?id=%s",
			self.basePath, http_util.encodeURIComponent(id));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_liquidity_pool_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_position_snapshots__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/positionSnapshots/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_position_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_positions__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/positions/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_position_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_reward_tokens__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/rewardTokens/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_reward_token_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_swaps__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/swaps/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_swap_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_tick_daily_snapshots__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/tickDailySnapshots/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_tick_daily_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_tick_hourly_snapshots__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/tickHourlySnapshots/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_tick_hourly_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_ticks__current(pool)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/ticks/current?pool=%s",
			self.basePath, http_util.encodeURIComponent(pool));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_tick_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_token_white_list_symbols__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/tokenWhiteListSymbols/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_token_white_list_symbol_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_token_white_lists__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/tokenWhiteLists/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_token_white_list_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_tokens__current(id)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/tokens/current?id=%s",
			self.basePath, http_util.encodeURIComponent(id));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_token_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_usage_metrics_daily_snapshots__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/usageMetricsDailySnapshots/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_usage_metrics_daily_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_usage_metrics_hourly_snapshots__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/usageMetricsHourlySnapshots/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_usage_metrics_hourly_snapshot_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function pancakeswapv3_ethereum_api:p_ancakeswapv3_ethereum_withdraws__current()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/dapps/pancakeswap-v3-ethereum/withdraws/current",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_pancakeswap_v3_ethereum_withdraw_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_pancakeswapv3_ethereum_api;
}
